# Batch H — distribution, observatory, video claim map

G1 is met (chi 0.777 / zod 0.911). See `evidence/10-H-results.md` (rewritten).
Observatory, video, Homebrew, and official marketplaces stay held until
P1 fixtures + P2 live transcripts. Do not treat this file as a start
order for those.

## Plan 03 surfaces

- Homebrew formula: `gridseak` binary + `install.sh` (aarch64 + x86_64).
- npm wrapper: `npx @gridseak/cli` → downloaded rust binary.
- Badge: `https://img.shields.io/badge/gridseak-local%20oracle-2ea44f`.

## Observatory (plan 05, compiler-tier languages only)

50 public repos, weekly scan, store scan_id + high_ratio_on_calls. Not stood up.

## Flagship video — claim → artifact

| Beat | Claim | Artifact |
|---|---|---|
| 1 | Compiler edges are labeled | `evidence_tier.rs` + MCP snapshot |
| 2 | No network at scan | `provision_index` exit 3 without cache |
| 3 | Disclosure is machine-readable | `disclosure_matrix.rs` |
| 4 | Calibration exists | `gridseak-truth-benchmark` ECE gate |
| 5 | Runtime can prove a call | `graphengine-runtime-witness` |
| 6 | Agent can be refuted | `verify_claim` |
| 7 | Human sees only what changed | report card + PR comment |
| 8 | One MCP server | `gridseak mcp --engine` |
| 9 | One-command install | `install.sh` + release.yml ARM |

Video file: not recorded.
