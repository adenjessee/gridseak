# P4 — deferred (do not start)

Dated 2026-09-16, updated after S3 n=1 + headless n=1.

`12-LIVE_GATE-results.md` now has operator rows (IDE n=1 and headless
mutation pack n=1). That unblocks *starting* calibration work. It does
not unblock marketplace submit.

Still deferred until a stranger can reproduce a deny (`curl | sh` on a
second machine, or an explicit OWNER `ABANDON` of that bar):

- Official Cursor / Anthropic marketplace
- Homebrew / npm
- Flagship video
- Observatory, type→vector docs, Apex god-file splits

Calibration, `dead()` once an Istanbul/go-cover fixture exists, and the
held-out “tests-green structural breaks” bench vs grep/Serena may start
as a *local* workstream. Do not publish G3 ECE as `p_true` until the
SCIP-as-oracle disclosure is replaced.

Do not grow the always-on MCP catalog.

Python G1 + 10 tasks only after TS/Go stay ≥ 0.60 on the existing
A/B pack.
